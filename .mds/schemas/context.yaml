# kotowari の IR の用語集（CONTEXT.md）の書式
name: ir-context
open: false
document:
  title:
    pattern: "^用語集$"
    extract: title
  preamble:
    statement:
      required: false
      extract: scope
    table:
      header: [用語, 意味, 出典]
      extract: glossary
  sections: []
