# TODO

`mds` の実装サイクル（改訂仕様への追従、ブランチ `mds`）で見つかった未解決事項の記録。

分類:
- **判断待ち**: 仕様の解釈や設計判断が要るもの。人が決めたら仕様と実装を直す
- **記録のみ**: 実害が小さい境界ケースや文言の細部。直すなら仕様の変更を伴う
- **v2 候補**: 仕様で「v1 の対象外」と明記したもの。実利用者が現れたら設計する

## 判断待ち

### ID 32: frontmatter が mapping でないときの扱い — 解決済み

- 状態: 仕様 R2 に「frontmatter が YAML のマッピングでないときは frontmatter_invalid で停止」と明記済み、実装も一致
- 判断（2026-09-17）: 現行が正しい。frontmatter は `field: value` の形式（マッピング）でなければ拒否する。`$schema <path>`（コロン無し）は mapping でないので停止。`$schema: <path>`（コロン有り）は通る

### ID 53: 題名より前の見出し — 解決済み

- 状態: 宣言済み前置部 + open のとき、題名より前の `### ` が undeclared_heading になる（修正前は open で合格）
- 判断（2026-09-17）: open モードでは題名より前の見出しを許す（未宣言の構造として）。閉じた世界では undeclared_heading のまま。open 設定で制御できる
- 例: `### 見出し3` を `# 見出し1` より前に置いた文書

### ID 58: `* 名前: 値`・`+ 名前: 値` のフィールド行判定 — 解決済み

- 判断（2026-09-17）: `-`、`*`、`+` のマーカーを同じ扱いに統一する（宣言済み名ならフィールド行、未宣言なら箇条書き）。仕様 R8 の文言を修正
- v2: 入れ子の要素も再帰的に処理する（別項目として TODO に記載）

### ID 59: 抽出要素の「元のマーカー行を保つ」の解釈 — 解決済み

- 判断（2026-09-17）: 抽出要素は元の行（空白込み）を維持する。実装は `{marker} {text}` の再構成ではなく元の行を使う

## 記録のみ

### ID 9: .gitignore

- `.mds/cache/` の除外を main の .gitignore に追加済み。確認のみ

### ID 12: セキュリティ次元

- 認証情報・ログ・ステージ対象に問題なし（確認の記録）

### ID 19: `mds --version` のテスト

- 実装は正しい。テストで固定していないだけ

### ID 23: 応答しないサーバのタイムアウト経路のテスト

- 固定値10秒のタイムアウトは実装済み。この経路だけテスト対象外。仕様がタイムアウト値を契約として宣言していない限りテスト再追加は Evidence conditions を満たさない

### ID 30: サブコマンドなしの argument_error 文言

- `mds` 単体で about 文がそのまま detail になる。エラーの説明として不自然

### ID 33: URL の userinfo がエラー文言に出る

- URL スキーマ取得失敗時、URL 全体を detail に埋め込む。userinfo（user:pass@）が標準エラーに出る。シークレットの扱いに関わる

### ID 34: キャッシュの SHA-256

- 仕様 R2 に SHA-256 を契約として明記済み。確認のみ

### ID 36: mapping でない frontmatter のテスト

- 実装は正しい。自動テストで固定していないだけ

### ID 38 / ID 60: detail 文言を assert するテスト

- テストが detail 文言（undeclared code block 等）を固定。仕様は detail の文言を契約にしていない。挙動を変えない文言変更で壊れる

### ID 41: 関数ドキュメントコメントが古い

- `blocks_from_list_item` の doc コメントが画像だけの段落の例外を反映していない

### ID 46: 空白のみの `$schema`

- 空白のみの `$schema` を frontmatter_invalid にする。仕様 R2 の「空のとき」の解釈を拡張している（空白のみも空）

### ID 47: 空末尾要素と継続段落の併存

- `- タグ: a,b,` + 継続段落 → `["a", "b", "\n継続"]`。R8 の空要素維持と R16 の末尾要素に付けるが両立しない

### ID 55: 未宣言構造の内側の `- 名前: 値` 行の detail 文言

- undeclared_line の detail が「undeclared field line」のまま。R8 の再定義では箇条書き扱いなので文言が不整合

### ID 62: 題名が無い文書の前置部領域の見出し

- 題名が無い文書ではすべての前置部領域の見出しが before_title=true になり、open では宣言済み前置部でも許される。仕様 R13 は題名無し文書の扱いを明記していない。missing_title が別に立つので実害は無い

### ID 63: マーカー単独行の lead 段落と継続段落の結合規則

- マーカー単独行（`- ` の行の後にインデントされた内容）の lead 段落と継続段落を両方持つ入力で、抽出要素の lead と継続段落の間が単一改行になる。仕様 R10 はこの入力形の結合規則（単一改行か空行か）を明記していない

## v2 候補

### ID 14: 入れ子リストの平坦化

- 仕様 R10 で「箇条書きの入れ子は v1 の対象外」と明記済み
- 親子関係（「決定の行とその補足」）を持つ文書を表現するには、Bullet に子ブロックを持たせる構造変更が必要
- 判断の記録（kotowari の `records.md`）を mds のスキーマで検査したい実利用者が現れたら設計する
- 追加（2026-09-17、ID 58 の判断から）: 入れ子の要素も再帰的に処理する（`-`、`*`、`+` の統一に続く v2 の課題）

## 会話メモ（U1）

- 依頼時に提示された ChatGPT 共有リンク（`<redacted>`）は JS 描画のため内容を取得できなかった。メモに仕様を変える内容があれば、この TODO を参照して仕様へ反映する

## 採用候補（次回向けの記録）

### ox-content のパーサ（`ox_content_parser`）

- 調査日: 2026-09-17。Markdown パーサの置き換え候補として調査・実験した結果
- **性能（実測）**: Rust 製の現行 `markdown` クレートとの比較で、パースは **60〜100 倍高速**（release、全ノード走査で揃えて測定）。ただし mds の実行時間に占めるパースの割合は約 31% なので、`mds check ./` の実質的な高速化は **1.45 倍程度**。200文書（約80KB）では体感できる差は出ない
- **品質（実測）**: CommonMark 0.31.2 の 652/652 例に完全準拠（CI で毎回検証）。ノード種類・行番号は現行 `markdown` クレートと一致（GFM の表・タスクリスト・strikethrough は `ParserOptions::gfm()` で有効化）
- **crates.io 公開済み**: `ox_content_parser` 3.2.8。依存は最小（bumpalo, thiserror, memchr, rustc-hash, compact_str, smallvec の7つ）。Rust 1.95+ 必要（現行 toolchain 1.98 で可）
- **置き換えのコスト（調査済み）**: `ast.rs` 書き直し（ox-content に serde が無いため `mds ast` の JSON を手書き）、`document.rs` 書き換え（Node 型差し替え・行番号ヘルパ追加）、frontmatter の切り出しを mds 側で新規実装（ox-content は frontmatter を AST ノードにしない）、インライン構造の違いへの対応
- **判断（2026-09-17）**: 現状維持。1.45 倍の高速化のために中〜大規模の書き直しとメンテナンスリスク（頻繁な API 変更）を負う価値が薄い
- **次回の参考**: 新規に Markdown パーサが必要なプロジェクト（または mds を大規模文書セットで使う要件が生じた場合）は、`ox_content_parser` の採用を検討する。アリーナ割当 + ゼロコピーでパースが極めて速い